schema {
  query: Query
  mutation: Mutation
}

scalar JsonData
scalar NumberReal

#########
## @Query

type Query {
  # api:status
  status: JsonData
  # api:tests
  demo: JsonData
}

############
## @Mutation

type Mutation {
  test: String!
}

